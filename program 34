import math
final_sum = 0
for i in range(145,999999):
    i = str(i)
    sum = 0
    for j in range(len(i)):
        sum += math.factorial(int(i[j]))
    if int(sum) == int(i):
        final_sum += sum
print(str(final_sum))
